'use babel';

import AtomSerialMonitorView from '../lib/atom-serial-monitor-view';

describe('AtomSerialMonitorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
